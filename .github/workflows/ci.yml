name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  build:
    permissions:
      contents: read
    uses: ./.github/workflows/build.yml
    with:
      push: false
      tags: servo:ci
